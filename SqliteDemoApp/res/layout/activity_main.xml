<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/buttonsignup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="36dp"
        android:onClick="registerActivity"
        android:text="Sign Up" />

    <Button
        android:id="@+id/buttonupdateprofile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/buttonlogin"
        android:layout_alignRight="@+id/buttonlogin"
        android:layout_below="@+id/buttonlogin"
        android:layout_marginTop="42dp"
        android:onClick="updateprofileActivity"
        android:text="Update Profile" />

    <Button
        android:id="@+id/buttondeleteprofile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/buttonupdateprofile"
        android:layout_alignRight="@+id/buttonupdateprofile"
        android:layout_below="@+id/buttonupdateprofile"
        android:layout_marginTop="42dp"
        android:onClick="deleteprofileActivity"
        android:text="Delete Profile" />

    <Button
        android:id="@+id/buttonlogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/buttonsignup"
        android:layout_alignRight="@+id/buttonsignup"
        android:layout_below="@+id/buttonsignup"
        android:layout_marginTop="31dp"
        android:onClick="loginActivity"
        android:text="Login" />

</RelativeLayout>
